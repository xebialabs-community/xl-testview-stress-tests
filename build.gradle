
allprojects { Project p ->
  group = "com.xebialabs.xltest"

  apply plugin: "eclipse"
  apply plugin: "idea"

  p.plugins.withType(JavaPlugin) {
    sourceCompatibility = 1.7
    targetCompatibility = 1.7
  }
}
